const cssPrefix = 'js-'
export const cssClasses = {
	cell: `${cssPrefix}board-cell`,
	row: `${cssPrefix}board-row`,
	cellLive: `${cssPrefix}cell-live`,
	cellDead: `${cssPrefix}cell-dead`,
	buttonPlay: `${cssPrefix}btn-play`,
	buttonReset: `${cssPrefix}btn-reset`,
	controlButtons: `${cssPrefix}control-btn`,
}

export const BOARD_SIZE = {
	x: 25,
	y: 70,
}
